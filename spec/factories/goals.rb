# Read about factories at https://github.com/thoughtbot/factory_girl

FactoryGirl.define do
  factory :goal do
    # w.weekly_results { rand.weekly_results }
    # w.weekly_points_goal { rand.weekly_points_goal }
    # category { Faker::Name.first_name }
    # point_value { Faker::Name.last_name }
    # frequency { rand(30).years.ago }
    # weekly_points_goal { Faker::Internet.email }
    # weekly_results { Faker::Internet.user_name }
  end
end
